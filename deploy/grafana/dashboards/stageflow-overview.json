{
  "annotations": {"list": []},
  "editable": true,
  "panels": [
    {"type": "timeseries", "title": "Active Runs", "gridPos": {"h": 8, "w": 8, "x": 0, "y": 0}, "targets": [{"expr": "active_runs", "legendFormat": "active"}]},
    {"type": "timeseries", "title": "HTTP Requests", "gridPos": {"h": 8, "w": 8, "x": 8, "y": 0}, "targets": [{"expr": "sum by (route, status) (rate(http_requests_total[5m]))", "legendFormat": "{{route}} {{status}}"}]},
    {"type": "timeseries", "title": "Worker Jobs", "gridPos": {"h": 8, "w": 8, "x": 16, "y": 0}, "targets": [{"expr": "sum by (event) (rate(worker_jobs_total[5m]))", "legendFormat": "{{event}}"}]},
    {"type": "timeseries", "title": "Flow Run Duration P95", "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8}, "targets": [{"expr": "histogram_quantile(0.95, sum by (le, status) (rate(flow_run_duration_seconds_bucket[5m])))", "legendFormat": "{{status}}"}]},
    {"type": "timeseries", "title": "Flow Step Duration P95", "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8}, "targets": [{"expr": "histogram_quantile(0.95, sum by (le, step, status) (rate(flow_step_duration_seconds_bucket[5m])))", "legendFormat": "{{step}} {{status}}"}]},
    {"type": "timeseries", "title": "Step Failures", "gridPos": {"h": 8, "w": 12, "x": 0, "y": 16}, "targets": [{"expr": "sum by (step, failure_kind) (rate(flow_step_failures_total[5m]))", "legendFormat": "{{step}} {{failure_kind}}"}]},
    {"type": "timeseries", "title": "Flow Runs Completed", "gridPos": {"h": 8, "w": 12, "x": 12, "y": 16}, "targets": [{"expr": "sum by (status) (rate(flow_runs_total[5m]))", "legendFormat": "{{status}}"}]}
  ],
  "schemaVersion": 39,
  "style": "dark",
  "tags": ["stageflow"],
  "templating": {"list": []},
  "time": {"from": "now-6h", "to": "now"},
  "title": "StageFlow Overview",
  "version": 1
}
